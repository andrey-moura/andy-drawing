#include <uva/drawing.hpp>

namespace uva
{
    namespace drawing
    {
        // window_surface::window_surface(size_t native_window)
        // {
        //     GtkWindow* window = reinterpret_cast<GtkWindow*>(native_window);

        //     int w, h;
        //     gtk_window_get_size(window, &w, &h);

        //     m_size.w = w;
        //     m_size.h = h;
        // }
    };
};